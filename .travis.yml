language: php
sudo: false

php: 7.2

cache:
  directories:
  - /home/travis/build/RoySegall/payme/vendor

before_script:
  - composer install

script:
  - ./vendor/bin/phpunit
  - ./vendor/bin/phpcs app --colors --standard=psr2